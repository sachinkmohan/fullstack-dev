# Teaching Notes

## Lesson pacing
- First lesson (0001) was too dense — user found it hard to follow.
- Prefer: one concept per lesson, analogy first, code second.
- Less code blocks per lesson. Fewer things to read before the quiz.
- Build from things the user already knows in JavaScript before introducing TypeScript syntax.
